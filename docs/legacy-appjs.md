# Legacy `app.js` Status

- `app.js` is deprecated and retained temporarily for historical/reference purposes.
- Active runtime paths are:
  - `main.js` -> `src/app-ui.js` for index mode
  - `main.js` -> `src/synth-ui.js` for synth mode
- Do not add new features to `app.js`.
- Removal/archive target: after parity sign-off and rollout acceptance.
