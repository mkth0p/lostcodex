# Legacy `app.js` Status

- `app.js` was deprecated and has now been removed from the active repository.
- Active runtime paths are:
  - `main.js` -> `src/app-ui.js` for index mode
  - `main.js` -> `src/synth-ui.js` for synth mode
- Contributor rule remains: do not add new runtime paths outside `main.js` mode routing.
